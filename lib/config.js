// Feature Flags Configuration
export const FEATURE_FLAGS = {
    // Note: MAINTENANCE_MODE is now dynamically controlled from the database via Admin Settings.
    // This value serves as a hard-coded fallback or legacy check.
    MAINTENANCE_MODE: false,
};
